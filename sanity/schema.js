import { agent } from "./schemas/agent";
import { category } from "./schemas/category";
import { property } from "./schemas/property";

export const schema = {
    types: [agent, property, category],
  }
  